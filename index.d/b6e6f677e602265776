*「& calc」のようなファイル名のファイルが存在するディレクトリで
  「echo `dir`」などを実行すると、電卓が起動してしまう

* ==「echo `pwd`」などバッククォートから「pwd」を実行した後、
   echoなどで出力する時に、次の行の先頭からでなく、カーソルの位置から出力される==

* ==行の表示限界を超えて文字を入力したあと^Wで削除した場合など、表示が乱れる==